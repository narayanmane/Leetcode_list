class Solution {
    boolean app(int n)
    {
        long i=1;
        while(i<n)
        {
            i=i*3;
        }
        return i==n;
    }
    public boolean isPowerOfThree(int n) {
        return app(n);
        
    }
}
