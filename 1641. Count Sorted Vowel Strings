class Solution {
public:
    int countVowelStrings(int n) {
    /* vector<int> ans(5,1);
        for(int i=2;i<=n;i++)
        {
            for(int i=3;i>=0;i--)
            {
                ans[i]=+ans[i+1];
            }
        }
        int res=0;
        for(auto a :ans)
            res+=a;
        return res; */
        return (n + 1) * (n + 2) * (n + 3) * (n + 4) / 24;
    }
};
